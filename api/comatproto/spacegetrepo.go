// Code generated by lexgen. DO NOT EDIT.

package comatproto

import (
	"context"
	"github.com/jcalabro/atmos/xrpc"
)

// Error name constants for SpaceGetRepo.
const (
	ErrSpaceGetRepo_SpaceNotFound   = "SpaceNotFound"
	ErrSpaceGetRepo_RepoNotFound    = "RepoNotFound"
	ErrSpaceGetRepo_RepoTakendown   = "RepoTakendown"
	ErrSpaceGetRepo_RepoSuspended   = "RepoSuspended"
	ErrSpaceGetRepo_RepoDeactivated = "RepoDeactivated"
)

// SpaceGetRepo calls the XRPC query "com.atproto.space.getRepo".
//
// Download an account's permissioned repo within a space as a CAR file, for full-state recovery. The CAR declares two roots in order: the signed commit, then a DRISL (DAG-CBOR) index mapping '{collection}/{rkey}' to record CID. Record blocks follow, in the same canonical DAG-CBOR key order as the index (length-first, then bytewise). Blobs are not included and are fetched separately via getBlob. Served by a repo host. Callable with either OAuth (for the authenticated user's own data) or a space credential (for syncing services).
func SpaceGetRepo(ctx context.Context, c *xrpc.Client, excludeValues bool, repo string, space string) ([]byte, error) {
	params := map[string]any{}
	if excludeValues {
		params["excludeValues"] = excludeValues
	}
	params["repo"] = repo
	params["space"] = space
	return c.QueryRaw(ctx, "com.atproto.space.getRepo", params)
}
